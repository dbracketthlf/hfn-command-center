import test from 'node:test';import assert from 'node:assert/strict';import { randomUUID } from 'node:crypto';import { Client } from 'pg';import { fundingAggregateSql } from '../src/storage/funding-aggregate.js';import { PostgresAriveRepository } from '../src/storage/postgres-arive.js';
const url=process.env.TEST_DATABASE_URL;
test('PostgreSQL funding aggregate deduplicates live funding and corrections',{skip:!url},async()=>{const client=new Client({connectionString:url}),schema=`hfn_funding_${randomUUID().replaceAll('-','')}`;await client.connect();try{await client.query(`create schema ${schema};set search_path to ${schema}`);await client.query('create table loans(id uuid primary key,arive_display_loan_id text,loan_amount numeric,processing_eligible_at timestamptz);create table loan_stage_events(id uuid primary key,loan_id uuid,event_type text,occurred_at timestamptz);create table historical_funding_corrections(display_loan_id text primary key,funded_date date,loan_amount numeric)');const add=async(id,display,amount,type,at)=>{await client.query('insert into loans values($1,$2,$3,$4) on conflict(id) do nothing',[id,display,amount,'2026-01-01']);await client.query('insert into loan_stage_events values($1,$2,$3,$4)',[randomUUID(),id,type,at]);};const live=randomUUID();await add(live,'LIVE-1',500000,'LOAN_FUNDED','2026-09-03T18:00:00Z');await add(live,'LIVE-1',500000,'LOAN_FUNDED','2026-09-04T18:00:00Z');await client.query("insert into historical_funding_corrections values('LIVE-1','2026-09-03',999999),('CORR-1','2026-09-10',250000),('AUG-1','2026-08-30',300000)");const result=await client.query(fundingAggregateSql,[2026,9]);assert.equal(result.rows[0].count,2);assert.equal(Number(result.rows[0].volume),750000);}finally{await client.query(`drop schema if exists ${schema} cascade`).catch(()=>{});await client.end();}});
test('PostgreSQL dashboard Funded MTD reuses the merged funding aggregate',{skip:!url},async t=>{const client=new Client({connectionString:url}),schema=`hfn_dashboard_funding_${randomUUID().replaceAll('-','')}`;await client.connect();try{await client.query(`create schema ${schema};set search_path to ${schema}`);await client.query(`create table loans(id uuid primary key,arive_display_loan_id text,loan_amount numeric,processing_eligible_at timestamptz,current_stage text,kpi_tracking_started_at timestamptz);create table loan_stage_events(id uuid primary key,loan_id uuid,event_type text,occurred_at timestamptz,received_at timestamptz,metadata jsonb);create table historical_funding_corrections(display_loan_id text primary key,funded_date date,loan_amount numeric);create table monthly_funding_goals(year smallint,month smallint,funded_loan_goal integer,funded_volume_goal numeric);create table assistant_tasks(loan_id uuid,task_type text,kpi_eligible boolean,applicable_at timestamptz,due_at timestamptz,completed_at timestamptz);create table sla_rules(id uuid primary key,metric_key text,owner_role text);create table sla_measurements(loan_id uuid,rule_id uuid,state text,due_at timestamptz,evaluated_at timestamptz)`);const add=async(display,amount,at)=>{const id=randomUUID();await client.query('insert into loans values($1,$2,$3,$4,$5,$6)',[id,display,amount,'2026-01-01','LOAN_FUNDED','2026-01-01']);await client.query('insert into loan_stage_events values($1,$2,$3,$4,$5,$6)',[randomUUID(),id,'LOAN_FUNDED',at,at,{}]);};await add('LIVE-1',500000,'2026-09-03T18:00:00Z');await client.query("insert into historical_funding_corrections values('LIVE-1','2026-09-03',999999),('CORR-1','2026-09-10',250000),('AUG-1','2026-08-30',300000)");await client.query('insert into monthly_funding_goals values(2026,9,30,15000000)');t.mock.timers.enable({apis:['Date'],now:new Date('2026-09-15T18:00:00Z')});const dashboard=await new PostgresAriveRepository(client).dashboard();assert.equal(dashboard.kpis.fundedMtd,2);assert.equal(dashboard.fundingGoal.fundedCount.actual,2);assert.equal(dashboard.kpis.fundedMtd,dashboard.fundingGoal.fundedCount.actual);assert.equal(dashboard.fundingGoal.fundedVolume.actual,750000);}finally{t.mock.timers.reset();await client.query(`drop schema if exists ${schema} cascade`).catch(()=>{});await client.end();}});
