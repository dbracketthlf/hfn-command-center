import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';
test('historical funding correction migration is funding-only and uniquely keyed',async()=>{const sql=await readFile(new URL('../db/010_historical_funding_corrections.sql',import.meta.url),'utf8');assert.match(sql,/display_loan_id text not null unique/);assert.match(sql,/loan_amount numeric\(14,2\) not null check \(loan_amount > 0\)/);assert.doesNotMatch(sql,/loan_stage_events|kpi_tracking|assistant_tasks|sla_measurements/i);});
test('funding backfill CLI validates input and writes only corrections',async()=>{const script=await readFile(new URL('../scripts/funding-backfill.mjs',import.meta.url),'utf8');assert.match(script,/historical_funding_corrections/);assert.match(script,/on conflict\(display_loan_id\) do update/i);assert.doesNotMatch(script,/loan_stage_events|assistant_tasks|sla_measurements|kpi_tracking/i);});
