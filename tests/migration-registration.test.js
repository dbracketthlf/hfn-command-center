import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';
test('normal migration paths register funding, Closing Disclosure, Victory, conditions workflow, and submission package migrations',async()=>{const [script,repository,migration,victory,conditions,packages]=await Promise.all([readFile(new URL('../scripts/migrate.mjs',import.meta.url),'utf8'),readFile(new URL('../src/storage/postgres-arive.js',import.meta.url),'utf8'),readFile(new URL('../db/011_closing_disclosure_sent.sql',import.meta.url),'utf8'),readFile(new URL('../db/014_victory_events.sql',import.meta.url),'utf8'),readFile(new URL('../db/019_conditions_follow_up_workflow.sql',import.meta.url),'utf8'),readFile(new URL('../db/020_underwriting_submission_packages.sql',import.meta.url),'utf8')]);assert.match(script,/applyMigrations/);assert.match(script,/postgresMigrationPoolOptions/);assert.match(script,/HFN_MIGRATION_EXTERNAL_DATABASE/);assert.match(repository,/010_historical_funding_corrections/);assert.match(repository,/011_closing_disclosure_sent/);assert.match(repository,/014_victory_events/);assert.match(repository,/019_conditions_follow_up_workflow/);assert.match(repository,/020_underwriting_submission_packages/);assert.match(migration,/closing_disclosure_sent/);assert.match(victory,/dedupe_key text not null unique/);assert.match(conditions,/workflow_cycle integer/i);assert.match(packages,/underwriting_submission_packages/);assert.match(packages,/unique \(loan_id, workflow_cycle\)/i);assert.match(packages,/item_type in \('appraisal','title','insurance','payoff','borrower_conditions'\)/i);assert.doesNotMatch(packages,/borrower_name|document_content|raw_payload|ssn|dob/i);assert.doesNotMatch(victory,/borrower|email|street|ssn|dob/i);});
