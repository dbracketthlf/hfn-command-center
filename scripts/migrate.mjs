import { Pool } from 'pg'; import { readFile } from 'node:fs/promises'; import { PostgresAriveRepository } from '../src/storage/postgres-arive.js'; import { postgresPoolOptions } from '../src/config/runtime.js';
if(!process.env.DATABASE_URL) throw new Error('DATABASE_URL is required for db:migrate');
const pool=new Pool(postgresPoolOptions({databaseUrl:process.env.DATABASE_URL,production:process.env.NODE_ENV==='production'}));
try{await new PostgresAriveRepository(pool).initialize();const applied=await pool.query('select name from schema_migrations where name=$1',['010_historical_funding_corrections']);if(!applied.rowCount){const client=await pool.connect();try{await client.query('begin');await client.query(await readFile(new URL('../db/010_historical_funding_corrections.sql',import.meta.url),'utf8'));await client.query('insert into schema_migrations(name) values($1)',['010_historical_funding_corrections']);await client.query('commit');}catch(error){await client.query('rollback');throw error;}finally{client.release();}}console.log('HFN database schema initialized.');}finally{await pool.end();}
