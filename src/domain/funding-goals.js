const pacificParts=now=>{const values=new Intl.DateTimeFormat('en-US',{timeZone:'America/Los_Angeles',year:'numeric',month:'numeric',day:'numeric',hour:'numeric',minute:'numeric',second:'numeric',hourCycle:'h23'}).formatToParts(now),get=type=>Number(values.find(value=>value.type===type)?.value);return {year:get('year'),month:get('month'),day:get('day'),hour:get('hour'),minute:get('minute'),second:get('second')};};
export const fundingGoalSummary=({year,month,goal,actualCount=0,actualVolume=0,missingAmountCount=0,now=new Date()})=>{const countGoal=goal?.fundedLoanGoal??null,volumeGoal=goal?.fundedVolumeGoal??null,parts=pacificParts(now),percent=(actual,target)=>target?Math.round(actual/target*100):null,current=parts.year===year&&parts.month===month,daysInMonth=new Date(Date.UTC(year,month,0)).getUTCDate(),elapsed=current?Math.round(((parts.day-1+(parts.hour+parts.minute/60+parts.second/3600)/24)/daysInMonth)*100):null,pace=value=>!current||value===null?'final':value>=elapsed?'on-pace':value>=Math.max(0,elapsed-10)?'at-risk':'behind';return {year,month,fundedCount:{actual:actualCount,goal:countGoal,percent:percent(actualCount,countGoal)},fundedVolume:{actual:actualVolume,goal:volumeGoal,percent:percent(actualVolume,volumeGoal),missingAmountCount},monthElapsedPercent:elapsed,filePace:pace(percent(actualCount,countGoal)),volumePace:pace(percent(actualVolume,volumeGoal))};};
